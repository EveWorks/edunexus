// import { useSession } from "next-auth/react";

// export const useUserData = () => {
//   const { data: session } = useSession() as any;
//   return session?.user?.data?.user;
// };

// export const useUserTokens = () => {
//   const { data: session } = useSession() as any;
//   return session?.user?.data?.tokens;
// };
